- name: Playbook de instalação do Agentes de hosts no Zabbix .
  hosts:
    - debian

      # VARIAVEIS DE AMBIENTE A SER PASSADAS SE CASO NAO HOUVE PROXY INFORMA ENDEREÇO SERVER ZABBIX.
  vars:

   # Se o  Agente for apontado diretamente para Zabbix Server informa em zbx_prox: IP Endereço do Ip do Zabbix server


    - proxy_name: 'PROXY-RJ'
    - zbx_proxy: 192.168.3.160
    - host_grupo: Matriz-B

    

  roles:
    - role: zabbix_agent_linux
